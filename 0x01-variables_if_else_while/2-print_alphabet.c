#include <stdio.h>
/**
  *main - Print alphabet in lowercase
  *
  *Return: always 0 (Success)
  */
int main(void)
{
	char a;

	for (a = 'a'; a <= 'd'; a++)
		putchar(a);

		putchar('\n');

	return (0);
}
