#include "main.h"
/**
 * factorial - returns the factorial of a given number
 * @n: the number to calculate factorial for
 *
 * Return: factorial for the given number,
 * or -1 if error
*/
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	if (n == 0)
	{
		return (1);
	}
}
