/**
 * _putchar - Writes a character to the standard output (stdout)
 * @c: The chacter to be written
 *
 * Return: On success, the character, the character written in returned
 * On error, -1 is returned and errno is set appropriately
 */
int _putchar(char c)
{
	return (_putchar (c));
}
